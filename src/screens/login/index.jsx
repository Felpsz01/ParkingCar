import { Text } from "react-native";
import { LoginContainer } from "./styles";

export default function Login() {
    return (
        <LoginContainer>
            <Text>Cade o texto do login</Text>
        </LoginContainer>
    );
}